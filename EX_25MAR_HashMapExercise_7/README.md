EX\_25MAR - HashMap exercise 7

Create a method named 'printInventoryEntrySet' that accepts a HashMap (with product names as keys and quantities as values read via Scanner).

The method should iterate over the HashMap using the 'entrySet()' method and print each key-value pair in the format:

&#x20;- Product: <product name>, Quantity: <quantity>

