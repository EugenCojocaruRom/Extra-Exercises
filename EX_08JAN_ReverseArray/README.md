EX\_08JAN\_Reverse Array

1. Have an array of type String. Return a copy of the array, but with the elements in reversed order. The original must remain unchanged.
2. Perform an in-place update of the array to get the array's elements in reversed order

&nbsp;- Define a helper method swapPositions(String\[] arr, int i, int j) to use for item 2.

