EX\_04FEB - 5 character words

Have an array of words and print a new array containing only the words longer than 5 characters.

Follow the following steps:

* Count how many words are >= 5 chars in the original array
* Create a new array with exactly that count as its size
* Fill the new array with only the words >= 5 chars
