EX\_23MAR\_HashMapExercise - keySet

Create a method named printInventoryKeySet that accepts a HashMap (where keys are product names and values are quantities) read via Scanner.

Your method should iterate over the HashMap using the keySet() method and print each key-value pair in the format:

&#x20;- Product: <product name>, Quantity: <quantity>

