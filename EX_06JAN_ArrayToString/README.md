EX\_06JAN\_Array to String

Return a string representation of an array of doubles.

The string should start with "\[" and end with "]" (even for an empty array), and then contain all the numbers (each formatted with just 2 decimals), separated by a comma and a space.

